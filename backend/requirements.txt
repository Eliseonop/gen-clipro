fastapi
uvicorn[standard]
yt-dlp
opencv-python-headless
numpy
pillow
faster-whisper
kokoro-onnx
soundfile
google-genai
mcp>=2,<3
openai
# Render de Motion Studio (motor HyperFrames). Tras instalar: python -m playwright install chromium
playwright
# Eliminar fondo (RVM, BiRefNet, U2-Net, SAM) y Kokoro TTS. Para GPU:
#   onnxruntime-gpu (NVIDIA/CUDA) u onnxruntime-directml (Windows, cualquier GPU)
onnxruntime
